# encoding UTF-8
require 'spec_helper'

describe AttachmentsMover do
  describe "when there are unordered files in /uploads" do
    it "should make a copy to a model specific folder" do
      pending
    end
  end
end